#!/bin/sh -e

%if browser != firefox
if [ "$1" = "remove" ]; then
    dpkg-divert --package @browser@ --rename --remove /usr/bin/firefox
fi
%endif

dpkg-maintscript-helper rm_conffile \
    /etc/@browser@/searchplugins/common/duckduckgo.xml 33.1-1~ @browser@ -- "$@"

#DEBHELPER#
